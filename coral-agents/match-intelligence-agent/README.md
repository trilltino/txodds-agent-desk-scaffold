# match-intelligence-agent

The only active Coral agent manifest in this repo.

This agent observes normalized TxLINE events, calls deterministic Rust tools,
requests proof context, and emits signals. Verification, payment amounts,
recipients, and settlement decisions remain code-owned.
