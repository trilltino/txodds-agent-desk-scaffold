# worldcup-buyer-agent

The buyer agent turns TxLINE triggers into market WANTs.

## Manifest

- `coral-agent.toml`: buyer identity, role, service name, and default bidding/verifier settings.

## Runtime Status

This archived manifest is currently mirrored by the compatibility registry in `src-tauri/src/services/coral/agents.rs`. New product work should build the Match Intelligence Agent instead of reviving buyer behavior.
