# seller-worldcup-edge

The edge seller specializes in fixture-bound World Cup odds movement.

## Manifest

- `coral-agent.toml`: seller identity, service name, delivery schema, and pricing/persona defaults.

## Runtime Status

Current behavior is implemented in Rust market strategy code. This manifest should become the source of truth for strategy parameters such as edge threshold, service allowlist, and delivery schema.
